/**
 * Module-level Dependencies augmentation. The kit's `billingProvider`
 * already declares the cradle keys; this stub exists so the auto-loader
 * picks up the directory under `modules/billing/` and so future
 * service-side billing-specific deps land in a predictable place.
 */
